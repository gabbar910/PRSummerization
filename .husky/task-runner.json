{
   "$schema": "https://alirezanet.github.io/Husky.Net/schema.json",
   "tasks": [
      {
         "name": "format-check",
         "group": "pre-commit",
         "command": "dotnet",
         "args": [ "format", "PRSummerization.sln", "--verify-no-changes", "--severity", "error" ],
         "include": [ "**/*.cs" ]
      }
   ]
}
